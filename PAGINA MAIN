import 'package:flutter/material.dart'; // declarando a biblioteca 

void main() {
  runApp(const MyApp()); //rodar aplicativo my app
}

class MyApp extends StatelessWidget { // chamando aplicativo my app

  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
    
      home:, //chamando a tela inicial do aplicativo
    );
  }
}
